# Implementation Tasks: frontend-extension-core

# Phase 3: Frontend Extension Core

## Extension Setup
- [x] 3.1 Configure `manifest.json` (Manifest V3) in `public/` directory.
- [x] 3.2 Update `vite.config.ts` for extension build compatibility.
- [x] 3.3 Set up global Dark Mode / Glassmorphism theme in `index.css`.

## Authentication UI
- [x] 3.4 Build `Auth` component for Login and Registration.
- [x] 3.5 Implement `chrome.storage.local` persistence for JWT.
- [x] 3.6 Connect Auth UI to the backend `AuthController`.

## Task Management & UI
- [x] 3.7 Build `TaskList` component to fetch and display tasks.
- [x] 3.8 Implement `QuoteBanner` for motivational quotes.
- [x] 3.9 Connect `TaskList` to the backend `TaskController`.
